[project]
name = "naver-local-search"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.3",
    "mcp[cli]>=1.6.0",
    "pip>=25.1",
    "python-dotenv>=1.1.0",
    "requests>=2.32.3",
]

[project.scripts]
naver-local-search = "main:main"